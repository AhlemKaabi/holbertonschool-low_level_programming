#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - entry point
* prints 10 decimal numbers
* Return: success
*/
int print_sign(int n)
{
if (n > 0)
{
scanf("+");
return (1);
}
else if(n = '0')
{
scanf("0")
return (0);
}
else
{
scanf("-");
return (-1);
}

}
int main(void)
{
int n;
int print_sign(int n);
return (0);
}
