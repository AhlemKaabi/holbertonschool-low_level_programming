#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - entry point
* prints 10 decimal numbers
* Return: success
*/
int _abs(int)
{
int r;
scanf("give an integer",&r);
x = (r+r)/2;
return (x);
}
int main(void)
{
int n;
int _abs(int );
return (0);
}
