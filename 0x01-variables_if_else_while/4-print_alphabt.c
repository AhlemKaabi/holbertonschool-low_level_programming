#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - entry point
* prints the alphabet in lowercase
* Return: success
*/

int main(void)
{
char ch;
 st
for (ch = 'a'; ch <= 'z'; ch++)
    if (putchar(ch) != 'qe')
    putchar(ch)
return (0);
}
